# No environment variables are consumed by apps/web yet (the frontend has
# no backend integration wired in — see the "Backend status" section of the
# root README). Phase 1 will add the Supabase client keys and the API base
# URL here as they're actually wired into the code, not before.
